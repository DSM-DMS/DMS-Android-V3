<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="dsm.android.v3">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".ui.main.MainActivity"
                android:label="@string/app_name">
        </activity>
        <activity
                android:name=".ui.pointLog.PointLogActivity"
                android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".ui.changePassword.ChangePasswordActivity">

        </activity>
        <activity android:name=".ui.applyGoing.ApplyGoingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.applyGoing.ApplyGoingLogActivity" />

        <activity android:name=".ui.applyGoing.ApplyGoingDocActivity"/>
    </application>

</manifest>